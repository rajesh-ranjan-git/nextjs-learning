import { Metadata } from "next";

// export const metadata: Metadata = {
//   title: "Auth",
//   default: "Next JS | Login",
//   template: "Next JS | %s",
// };

const AuthLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default AuthLayout;
